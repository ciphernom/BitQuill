# Dependencies
# This directory contains all downloaded packages and can be huge.
# It should always be regenerated by running `npm install`.
/node_modules

# Webpack Build output
# This is the final bundled code for deployment, not source code.
/dist/

# Rust / WASM build artifacts
# These are temporary files generated during compilation.
/vdf-wasm/target/
/vdf-wasm/pkg/

# Security - Private Keys
# Never commit private keys or sensitive credentials to a repository.
/vdf-wasm/test_key.pem

# OS-specific files
.DS_Store
Thumbs.db
